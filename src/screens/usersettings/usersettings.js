import React from "react"
import {
    SafeAreaView,
    View,
    Text,
    TouchableOpacity,
    Image,
    FlatList
} from "react-native"
import { Header } from '../../components'

const UserSettings = ({navigation}) => {
    

    return (
        <SafeAreaView>
            <Header navigation={navigation} backBtn={true} />
            <Text>UserSettings Screen</Text>
            {/* <Header /> */}
            {}
            {}
            {}
        </SafeAreaView>
    )
}

export default UserSettings