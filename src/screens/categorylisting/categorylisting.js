import React from "react"
import {
    SafeAreaView,
    View,
    Text,
    TouchableOpacity,
    Image,
    FlatList
} from "react-native"
import { Header } from '../../components'

const CategoryListing = ({navigation}) => {
    

    return (
        <SafeAreaView>
            <Header navigation={navigation} backBtn={true} />
            <Text>CategoryListing Screen</Text>
            {/* <Header /> */}
            {}
            {}
            {}
        </SafeAreaView>
    )
}

export default CategoryListing