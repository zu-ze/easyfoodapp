import React from "react"
import {
    SafeAreaView,
    View,
    Text,
    TouchableOpacity,
    Image,
    FlatList
} from "react-native"
import { Header } from '../../components'

const UserFavourites = ({navigation}) => {
    

    return (
        <SafeAreaView>
            <Header navigation={navigation} backBtn={true} />
            <Text>UserFavourites Screen</Text>
            {/* <Header /> */}
            {}
            {}
            {}
        </SafeAreaView>
    )
}

export default UserFavourites